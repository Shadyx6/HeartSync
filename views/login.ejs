<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HeartSync - Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background: linear-gradient(135deg, #FF8A8A, #FFC8DD);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .login-container {
      background: white;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      padding: 2rem;
      display: flex;
      align-items: center;
      max-width: 700px;
      width: 100%;
    }
    .avatar-circle {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      background-color: #ffe6e6;
      border: 4px solid #ff5f6d;
      display: flex;
      justify-content: center;
      align-items: center;
      margin-bottom: 1rem;
    }
    .avatar-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    .avatar-option {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      border: 2px solid #ff5f6d;
      cursor: pointer;
      transition: transform 0.2s ease;
    }
    .avatar-option:hover {
      transform: scale(1.1);
    }
    .input-group {
      margin-bottom: 1.5rem;
      width: 100%;
    }
    .username-input {
      width: 100%;
      padding: 0.75rem;
      border: 2px solid #ff5f6d;
      border-radius: 10px;
      background-color: #ffe6e6;
    }
    .login-button {
      width: 100%;
      padding: 0.75rem;
      border-radius: 10px;
      background-color: #ff5f6d;
      color: white;
      font-weight: bold;
      border: none;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    .login-button:hover {
      background-color: #cc4c5c;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="flex-1 flex flex-col items-center justify-center">
        <h1 class="text-4xl">Welcome to heart<span class=" text-pink-500">Sync💕</span></h1>
        <p class=" mt-4">You are just a click away from your private Video calling Experience</p>
      <div class="input-group flex items-center justify-center ">
          <button onclick="window.location.href='/live-chat'" type="submit" class="mt-6 login-button w-1/4 items-center justify-center">Join HeartSync</button>
        </form>
      </div>
    </div>
</body>
</html>
